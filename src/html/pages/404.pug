<!doctype html>

html(lang="en")
    head
        meta(charset="utf-8")
        title Page Not Found
        meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")

        if environment == 'demonstration'
            meta(name="robots", content="noindex, nofollow")

        style.
            * {
                line-height: 1.2;
            }

            html {
                display: table;
                width: 100%;
                height: 100%;
                font-family: sans-serif;
                color: #888;
                text-align: center;
            }

            body {
                display: table-cell;
                vertical-align: middle;
                padding: 1rem;
            }

            h1 {
                color: #555;
                font-size: 2rem;
                font-weight: 700;
            }

            h1,
            p {
                margin-top: 0;
                margin-bottom: .5rem;
            }

            small {
                font-size: .7em;
            }

    body
        h1 404
            br
            small Page Not Found

        p Sorry, but the page you were trying to view does not exist.
        p Check your URL address or go to the <a href="/">main&nbsp;page</a> of the site.
